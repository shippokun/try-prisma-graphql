# Try-Prisma-GraphQL

PrismaORM と GraphQL のテスト

## 作業

```shell
// dbの立ち上げ
$ docker compose up -d
```

```shell
// migration
$ npm run db:migrate

// seed
$ npm run db:seed

// reset
$ npm run db:reset
```
